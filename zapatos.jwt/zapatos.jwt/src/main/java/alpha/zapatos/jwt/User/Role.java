package alpha.zapatos.jwt.User;

// Enumeración que define los roles disponibles para los usuarios.
public enum Role {
    ADMIN,  // Rol de administrador, con acceso completo a las funcionalidades.
    USER    // Rol de usuario regular, con acceso limitado a las funcionalidades.
}
